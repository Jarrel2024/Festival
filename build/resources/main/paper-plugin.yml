name: Festival
version: '1.0-SNAPSHOT'
main: org.zzzzzzz.festival.Festival
api-version: '1.21'
dependencies:
  bootstrap:
    WorldEdit:
      load: BEFORE
      required: true
      join-classpath: true
  server:
    WorldEdit:
      load: BEFORE
      required: true
      join-classpath: true
WorldEdit:
  load: BEFORE
  required: true
